# 0.3.6.96

Исправление семи issues #214, #210, #204, #200, #188, #174, #163: компактный режим больше не «прыгает» при появлении новых строк дерева баз, удалён оставшийся мёртвый файл окна тегов без точки входа, изолированная регистрация горячих клавиш с отбраковкой значений без модификатора, своевременное обновление кнопки «Смена пользователя», защита `settings.json` от одного испорченного числа NaN/∞, понятные сообщения диалога определения свойств конфигурации и корректное разделение адреса хранилища на оба разделителя при миграции со StartManager.

## Исправлено

- **Компактный режим больше не «прыгает»** (#214): строки дерева баз, появляющиеся после первичного применения компакт-режима (фоновая инициализация, виртуализация/прокрутка, пересборка после сохранения свойств базы), теперь тоже компактизируются. В `Themes/ThemeManager.cs` добавлен `ApplyCompactTree`, в `Views/MainWindow.Columns.cs` строки базы/группы применяют компакт-режим, а в `Views/MainWindow.xaml.cs` пересчитывается выравнивание колонок после пересборки дерева.
- **Удалён оставшийся мёртвый файл окна тегов** (#210): разметка `TagInputWindow` была удалена ранее, поэтому класс стал неработоспособен; удалён `Views/TagInputWindow.Avalonia.cs`.
- **Горячие клавиши: значения без модификатора отбраковываются, регистрация изолирована** (#204): при чтении настроек буквы/цифры без модификатора отклоняются (`TryParseKeyGesture`, `IsAllowedWithoutModifier`); каждая привязка регистрируется изолированно, регистрация хоткеев вынесена из общего `try` — старое значение больше не отключает Alt+1…9, восстановление последней базы и выравнивание заголовка.
- **Кнопка «Смена пользователя» обновляется после изменения списка профилей** (#200): видимость кнопки пересчитывается по событию `ProfilesChanged` в `IProfileService`/`ProfileService`; окно выбора учётной записи (`Views/LoginWindow.xaml.cs`) применяет активную тему/скин.
- **Одно испорченное число больше не роняет весь settings.json** (#188): в `Services/InfobaseRepository.cs` добавлен `NumberHandling = JsonNumberHandling.AllowNamedFloatingPointLiterals`, чтобы значение NaN/∞ в одном поле не ломало весь файл; чтение настроек согласовано с этим.
- **Понятные сообщения при определении свойств конфигурации** (#174): диалог прогресса и сообщение об ошибке теперь показывают конкретный КОМ-коннектор (ProgID) и версию платформы; подтверждено, что при импорте баз определение свойств запускается только по явной команде, а не автоматически.
- **Импорт из StartManager: адрес хранилища делится на сервер и имя хранилища по обоим разделителям** (#163): `StartManagerImporter.BuildRepository` учитывает и `/`, и `\`.

## Файлы

- `Themes/ThemeManager.cs` — добавлен `ApplyCompactTree` (issue #214).
- `Views/MainWindow.Columns.cs` — строки базы/группы применяют компакт-режим (issue #214).
- `Views/MainWindow.xaml.cs` — пересчёт выравнивания колонок после пересборки дерева (issue #214).
- Удалён `Views/TagInputWindow.Avalonia.cs` — мёртвое окно без точки входа (issue #210).
- `Controls/HotkeyBox.cs`, `Views/SettingsWindow.Hotkeys.cs` — отбраковка значений без модификатора, изолированная регистрация хоткеев (issue #204).
- `Services/ProfileService.cs`, `Views/LoginWindow.xaml.cs` — событие `ProfilesChanged`, применение активной темы (issue #200).
- `Services/InfobaseRepository.cs` — `NumberHandling = AllowNamedFloatingPointLiterals` (issue #188).
- `Services/DetectConfigProgressWindow*.cs` — конкретный КОМ-коннектор и версия платформы в сообщениях (issue #174).
- `Services/StartManagerImporter.cs` — разделение адреса хранилища на `/` и `\` (`BuildRepository`, issue #163).
- `Configuration Management.csproj` — версия `0.3.6.95` → `0.3.6.96` (4 поля).
- `CHANGELOG.md`, `README.md` — запись о выпуске и бейдж версии.