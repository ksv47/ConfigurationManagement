# 0.3.6.92

Реализация ISSUE #201 «Пара пожеланий»: новое действие «Свернуть» (не в трей) после запуска базы/конфигуратора, режим запуска базы по умолчанию («1С:Предприятие»/«Конфигуратор») при двойном клике и быстрая авторизация для Конфигуратора «как для 1С:Предприятия».

## Добавлено

- **Действие «Свернуть» (не в трей) после запуска базы/конфигуратора** (#201): в настройку «После запуска базы или конфигуратора» добавлен режим «Свернуть» — главное окно сворачивается в панель задач, оставаясь в ней (в отличие от «Свернуть в трей»). Реализовано в `Models/AfterLaunchAction.cs` (новое значение `Minimize`) и в обработчиках `Views/MainWindow.Tray.cs` (WPF) и `Views/MainWindow.Avalonia.cs` (Linux), а также в выпадающих списках `Views/SettingsWindow.Display.cs`, `Views/SettingsWindow.Platforms.cs` и `Views/SettingsWindow.Avalonia.cs`.
- **Режим запуска базы по умолчанию** (#201): в окне настроек базы («Запуск») появилось поле «Режим запуска по умолчанию» — «Автоматически (1С:Предприятие)», «1С:Предприятие» или «Конфигуратор». При двойном клике на базе она открывается в указанном режиме: WPF — `Views/MainWindow.Events.cs` (`OnInfobaseTree_PreviewMouseDoubleClick`), Avalonia — карточка строки в `Views/MainWindow.Avalonia.cs`. Значение хранится в `Models/Infobase.cs` (`DefaultLaunchMode`) и переносится через `ViewModels/ConnectionSettingsViewModel.cs`.
- **Быстрая авторизация для Конфигуратора «как для 1С:Предприятия»** (#201): в окне настроек подключения на вкладке «Авторизация» в группе Конфигуратора появился флаг «Авторизация как для 1С:Предприятия». При включении поля авторизации Конфигуратора блокируются, а в его настройки копируются логин, пароль и режим входа «1С:Предприятия». UI: `Views/ConnectionSettingsWindow.xaml` (WPF) и `Views/ConnectionSettingsWindow.Avalonia.cs`; признак хранится в `Models/Infobase.cs` (`ConfiguratorUseEnterpriseAuth`) и применяется в `ViewModels/ConnectionSettingsViewModel.cs` (`ApplyTo`).
- Локализация новых строк в `Localization/Languages/ru.json` и `en.json`.

## Файлы

- `Models/AfterLaunchAction.cs` — новое значение `Minimize`, `ToSettingString`/`Parse` (issue #201).
- `Views/MainWindow.Tray.cs`, `Views/MainWindow.Avalonia.cs` — обработка действия «Свернуть» после запуска.
- `Views/SettingsWindow.Display.cs`, `Views/SettingsWindow.Platforms.cs`, `Views/SettingsWindow.Avalonia.cs` — пункт «Свернуть» в списке действий после запуска.
- `Models/Infobase.cs` — поля `DefaultLaunchMode` и `ConfiguratorUseEnterpriseAuth`.
- `ViewModels/ConnectionSettingsViewModel.cs` — свойства `DefaultLaunchMode` и `ConfiguratorUseEnterpriseAuth`, их загрузка/сохранение и копирование учётных данных Конфигуратора из «1С:Предприятия».
- `Views/MainWindow.Events.cs`, `Views/MainWindow.Avalonia.cs` — запуск базы в режиме по умолчанию при двойном клике.
- `Views/ConnectionSettingsWindow.xaml`, `Views/ConnectionSettingsWindow.xaml.cs`, `Views/ConnectionSettingsWindow.Avalonia.cs` — поле «Режим запуска по умолчанию» и флаг «Авторизация как для 1С:Предприятия».
- `Localization/Languages/ru.json`, `en.json` — новые строки локализации.
- `Configuration Management.csproj` — версия `0.3.6.91` → `0.3.6.92` (4 поля).
- `CHANGELOG.md`, `README.md` — запись о выпуске и бейдж версии.