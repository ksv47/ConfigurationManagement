# 0.3.6.80

Исправление ISSUE #178 «Окно Очистка кэша 1С: размеры по базам всегда 0, очистка не находит кэш, остатки не удаляются». Переписано определение каталогов кэша 1С: каталоги ищутся по карте `IdConnStrMap` из файла `1cv8u.pfl`, которую платформа ведёт в корне пользовательского кэша, а не по имени базы/ID (платформа называет каталог кэша собственным GUID, не связанным с базой). Правка внесена в общий сервис очистки — работает в обеих реализациях (Windows/WPF и Linux/Avalonia).

## Исправлено

- **Размеры кэша по базам показывали 0 Б**: каталоги кэша теперь берутся из карты `IdConnStrMap` файла `1cv8u.pfl` (`%APPDATA%\1C\1cv8` на Windows, `~/.1cv8/1C/1cv8` на Linux) как все GUID-каталоги, сопоставленные строке соединения базы. Файл читается с учётом UTF-8 BOM и CRLF, результат кэшируется; отсутствие файла или пустая карта обрабатываются без падения.
- **Очистка сообщала «кэш не найден», хотя каталог есть на диске**: поиск и удаление выполняются по реальным GUID-каталогам из карты, в том числе для всех вариантов написания хоста (без порта/с портом).
- **«Остатки от удалённых баз» не удалялись**: `BuildProtectedNames` защищает каталоги живых баз (GUID из карты + имена `Srvr__…__Ref__…__` + ID и имя базы), поэтому осиротевшие каталоги определяются корректно и удаляются.
- **Второй каталог клиент-серверной базы на Linux** (`Srvr__<сервер>__Ref__<база>__`) учитывается в расчёте размера и очистке этой базы.
- **Хост сравнивается без учёта порта с обеих сторон** (`NormalizeHost`): порт может быть отдельным полем или вписанным в имя сервера.

## Файлы

- `Services/OneCCacheCleaner.cs` — переписанное определение каталогов кэша по карте `IdConnStrMap` (`1cv8u.pfl`).
- `Configuration Management.csproj` — версия `0.3.6.79` → `0.3.6.80` (4 поля).
- `CHANGELOG.md`, `README.md` — запись о выпуске и бейдж версии.