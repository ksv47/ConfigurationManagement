# 0.3.6.98

Исправление issue #174 «Кнопка определения свойств конфигурации» по замечаниям пользователя: в сообщении об ошибке чтения через COM больше не пустое имя базы («» вместо реального имени), таймаут определения свойств стал настраиваемым (по умолчанию 30000 мс вместо жёстких 8000 мс), а при определении свойств база с непустым именем корректно попадает в журнал.

## Исправлено

- **Пустое имя базы в сообщении об ошибке** (#174): `BuildProbeInfobase` в `ViewModels/ConnectionSettingsViewModel.cs` теперь заполняет имя базы (приоритет: заданное наименование → `Ref`/`DatabaseName` → имя файла), а в `Services/OneCComConnector.cs` добавлен защитный `DisplayName`, подставляющий осмысленное имя вместо пустой строки «» во всех сообщениях и записях журнала.
- **Настраиваемый таймаут определения свойств конфигурации** (#174): таймаут вынесен из жёстких 8000 мс в настройку `ComDetectTimeoutMs` (`Models/AppSettings.cs`, по умолчанию **30000 мс**, минимум 1000). Значение резолвится в `Services/ConfigurationInfoService.cs` и доходит до агента `ComReadHost`, поэтому первое COM-подключение к клиент-серверной базе (холодный старт сервера, лицензии, создание сеанса) больше не обрывается на 8-й секунде. В журнал при ошибке дополнительно пишется применённый таймаут.
- **Настройка таймаута в окне настроек** (#174): поле «Таймаут определения свойств конфигурации (мс)» добавлено в `Views/SettingsWindow.xaml` и `Views/SettingsWindow.Avalonia.cs`; значение сохраняется через `MainViewModel.ComDetectTimeoutMs` (Windows/WPF и Linux/Avalonia).
- **Удалён мёртвый код фонового автодочитывания** (#174): удалён неиспользуемый `RefreshConfigurationInfoAsync` и связанное поле `_configInfoFailedKeys` в `ViewModels/MainViewModel.Tools.cs` — чтение свойств выполняется только по явной команде («Обновить информацию» или кнопка «Определить»), а не при старте/импорте.

## Файлы

- `ViewModels/ConnectionSettingsViewModel.cs` — имя базы в `BuildProbeInfobase` (issue #174).
- `Services/OneCComConnector.cs` — защитный `DisplayName`, логирование таймаута (issue #174).
- `Models/AppSettings.cs` — настройка `ComDetectTimeoutMs` (issue #174).
- `Services/ConfigurationInfoService.cs` — резолв таймаута из настроек (issue #174).
- `ViewModels/MainViewModel.cs`, `MainViewModel.Commands.cs`, `MainViewModel.Launch.cs`, `MainViewModel.Avalonia.cs` — свойство `ComDetectTimeoutMs`.
- `Views/SettingsWindow.xaml`, `Views/SettingsWindow.xaml.cs`, `Views/SettingsWindow.Avalonia.cs` — поле настройки таймаута (issue #174).
- `Localization/Languages/ru.json`, `en.json` — ключи поля таймаута.
- `ViewModels/MainViewModel.Tools.cs` — удалён мёртвый код автодочитывания (issue #174).
- `Configuration Management.csproj` — версия `0.3.6.97` → `0.3.6.98` (4 поля).
- `CHANGELOG.md`, `README.md` — запись о выпуске и бейдж версии.