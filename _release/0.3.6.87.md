# 0.3.6.87

Реализация ISSUE #200 «Смена пользователя»: в работающее приложение добавлена отдельная кнопка «Смена пользователя» на верхней панели рядом с настройками и настраиваемая горячая клавиша. Кнопка видна только при наличии нескольких учётных записей; по ней открывается тот же диалог выбора/входа, что и при запуске, но без перезапуска программы — при успехе активный профиль переключается и данные главного окна (список баз, группы, избранное, тема, язык, горячие клавиши) перезагружаются, при отмене состояние не меняется.

## Добавлено

- **Кнопка «Смена пользователя»** (#200): добавлена на верхнюю панель рядом с кнопкой настроек в `Views/MainWindow.xaml` (Windows/WPF) и в сборку панели в `Views/MainWindow.Avalonia.cs` (Linux/Avalonia). Видна только при `Profiles.Count > 1` (привязка к новому свойству `SwitchUserVisible`).
- **Смена пользователя без перезапуска** (#200): команда `SwitchUserCommand` (`ViewModels/MainViewModel.SwitchUser.cs` для WPF, команда в `ViewModels/MainViewModel.Avalonia.cs` для Avalonia) показывает тот же диалог `LoginWindow.ShowLogin`, что и при запуске; при успехе вызывает `profileService.SetCurrentProfile(...)` и перезагружает данные главного окна (`ReloadAllData` / `ReloadAfterProfileSwitch` — список баз, группы, избранное, свёрнутые группы, тему/схему, язык и горячие клавиши). Отмена или выбор текущей записи ничего не меняют.
- **Настраиваемый хоткей «Смена пользователя»** (#200): новое поле `HotkeySwitchUser` (`Models/AppSettings.cs`) и его настройка на вкладке «Клавиши» (`Views/SettingsWindow.xaml` и `Views/SettingsWindow.Hotkeys.cs` — WPF; `Views/SettingsWindow.Avalonia.cs` — Avalonia). Регистрация привязки — `Views/MainWindow.Hotkeys.cs` и `Views/MainWindow.Avalonia.cs`.
- **Локализация**: ключи `Main.SwitchUser`, `Main.SwitchUserTooltip`, `Settings.Hotkeys.SwitchUser` добавлены в `Localization/Languages/ru.json` и `Localization/Languages/en.json`.

## Файлы

- `ViewModels/MainViewModel.SwitchUser.cs` — новый partial-файл WPF: `SwitchUserCommand`, `HotkeySwitchUser`, `SwitchUserVisible`, `SwitchUser()`, `ReloadAllData()`.
- `ViewModels/MainViewModel.cs`, `ViewModels/MainViewModel.Launch.cs`, `ViewModels/MainViewModel.Tools.cs` — поле/чтение и сохранение `HotkeySwitchUser`, параметр `hotkeySwitchUser` в `ApplyAppBehaviorSettings`.
- `ViewModels/MainViewModel.Avalonia.cs` — `HotkeySwitchUser`, `SwitchUserCommand`, `SwitchUserVisible`, `SwitchUser()`, `ReloadAfterProfileSwitch()`, правки `ApplyHotkeys`.
- `Views/MainWindow.xaml`, `Views/MainWindow.Avalonia.cs` — кнопка «Смена пользователя».
- `Views/MainWindow.Hotkeys.cs`, `Views/MainWindow.Avalonia.cs` — регистрация хоткея.
- `Views/SettingsWindow.xaml`, `Views/SettingsWindow.Hotkeys.cs`, `Views/SettingsWindow.xaml.cs`, `Views/SettingsWindow.Avalonia.cs` — строка и сохранение хоткея «Смена пользователя».
- `Models/AppSettings.cs` — поле `HotkeySwitchUser`.
- `Localization/Languages/ru.json`, `Localization/Languages/en.json` — новые ключи.
- `Configuration Management.csproj` — версия `0.3.6.86` → `0.3.6.87` (4 поля).
- `CHANGELOG.md`, `README.md` — запись о выпуске и бейдж версии.