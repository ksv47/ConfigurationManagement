# Управление конфигурациями 1С

![Версия](https://img.shields.io/badge/Версия-0.3.4.5-1F6FEB) ![.NET](https://img.shields.io/badge/.NET-10-512BD4) ![Windows/WPF](https://img.shields.io/badge/Windows-WPF-4B8BBE) ![Linux/Avalonia](https://img.shields.io/badge/Linux-Avalonia%2011-8B5CF6) ![Лицензия](https://img.shields.io/badge/Лицензия-Open%20Source-success)

> Десктопное приложение для управления информационными базами 1С:Предприятие 8.3. **Версия 0.3.4.5**

**Управление конфигурациями 1С** — это кроссплатформенное настольное приложение на платформе **.NET** (**WPF** на Windows и **Avalonia 11** на Linux), которое заменяет стандартный список баз 1С и предоставляет современный интерфейс для работы с информационными базами: запуск в режимах «1С:Предприятие» и «Конфигуратор», вкладки **Все базы / Избранное / Недавние**, иерархические группы, избранное с `Alt+1…9`, мультифильтр по тегам, очистка кеша, синхронизация с `ibases.v8i`, системный трей, светлая и тёмная темы. Одна кодовая база собирается под обе ОС: `net10.0-windows` (WPF) на Windows и `net10.0` (Avalonia) на Linux.

[![Infostart](https://infostart.ru/bitrix/templates/sandbox_empty/assets/tpl/abo/img/logo.svg)](https://infostart.ru/1c/articles/2764888/)

> 📌 **Публикация на Infostart:** [Управление конфигурациями 1С](https://infostart.ru/1c/articles/2764888/)

---

## 📜 Лицензия и открытый исходный код

Проект распространяется с **открытым исходным кодом** и доступен бесплатно. Исходный код размещён в разделе **Файлы** публикации на [Infostart](https://infostart.ru/1c/articles/2764888/), а также в этом репозитории GitHub.

---

## ✨ Возможности

### 🚀 Запуск информационных баз
- Запуск базы в режиме **«1С:Предприятие»** и **«Конфигуратор»**.
- **Свободно назначаемые горячие клавиши** (Настройки → Клавиши), как в конфигураторе 1С: 1С:Предприятие, Конфигуратор, Избранное, Изменить, Удалить, Очистить кэш, Добавить базу, Закрепить (по умолчанию `F3` / `F4`). Для назначения достаточно установить курсор в поле и нажать сочетание — поддерживаются **Ctrl, Shift, Alt, Win** (например `Ctrl+Shift+F`), а также F2–F12, Delete, Insert; `Backspace`/`Delete` сбрасывают, `Esc` отменяет ввод. Отображаются на кнопках правой панели и в контекстном меню.
- **Горячие клавиши вкладок списка баз** — отдельные комбинации для переключения **Все базы / Избранное / Недавние** (задаются в **Настройки → Клавиши**).
- **`Ctrl+F`** — фокус в поле поиска (с выделением текущего текста) из любого места главного окна.
- **Горячие клавиши избранного** — `Alt+1`…`Alt+9`: при добавлении в избранное назначается свободный слот; порядок слотов настраивается в окне настроек.
- Выбор типа клиента: **тонкий** / **толстый**, режим **веб-клиента** (доступен только при подключении базы через **веб-сервер**).
- **Толстый клиент разделён по режиму форм** в настройках базы (вкладка **«Запуск»**): **«Толстый клиент (управляемые формы)»** (`/RunModeManagedApplication`) и **«Толстый клиент (обычные формы)»** (`/RunModeOrdinaryApplication`). Это решает случай, когда база на управляемых формах запускалась в обычных.
- **Разрядность запуска** по документации 1С — 4 режима: приоритет 32 (x86) / приоритет 64 (x86-64) / только 32 / только 64; при приоритетных режимах лаунчер выбирает более новую установленную версию между 32/64.
- **Разрядность по умолчанию** в **Настройки → Платформы**: если у базы не указана собственная разрядность, запуск выполняется выбранной разрядностью (**64 бит** по умолчанию или **32 бит**). Применяется к запуску «1С:Предприятие», «Конфигуратор», пакетным операциям и ярлыкам.
- **Разделение «Толстый клиент» по режиму форм в «Текущей сессии»** в правой панели — по аналогии с настройками подключения базы 1С: **«Толстый (управляемые формы)»** (`/RunModeManagedApplication`) и **«Толстый (обычные формы)»** (`/RunModeOrdinaryApplication`). Выбор применяется только к следующему запуску «1С:Предприятие», настройки базы при этом не изменяются.
- Автопоиск установленной платформы 1С (лаунчер `1CEStart.exe` или `1cv8.exe` / `1cv8c.exe`).
- Запуск **конкретной версии** платформы, заданной для базы.
- **Окно выбора версии платформы** как в стартере 1С: дерево **полностью разворачивается** при открытии, свежие версии **по умолчанию сверху** (сортировка по убыванию, переключение иконками как в главном окне), **текущая версия базы подсвечивается жирным** и автоматически позиционируется. Если у базы указана **частичная версия** (`8.3`, `8.3.19` или с разрядностью `8.3 (64)`), выбирается **максимальная доступная сборка** — как это делает сам лаунчер 1С.
- **Дополнительные папки платформ** (Настройки → Платформы): версии из нестандартных/портативных каталогов установки (произвольная вложенность без обязательного подкаталога `1cv8`) находятся, показываются в списке и **корректно запускаются** — и для конкретной версии базы, и при выборе новейшей установленной версии, и в приоритетных режимах разрядности.
- **«Перейти по ссылке»** в правой панели — аналог одноимённой функции стандартного загрузчика 1С: вставьте ссылку на базу (`e1c://...`, `C:\1C\База`, `server\База`, `http://server/base`) и запустите её в «1С:Предприятие».
- **Конфигуратор параметров запуска** — окно «Параметры запуска» устроено как **поле ввода + справочник**: вверху — поле ввода параметров командной строки, ниже — справочник всех ключей запуска 1С с описанием, что делает параметр. Двойной клик по строке справочника подставляет параметр в поле ввода (`/UC`, `/DisableStartupMessages`, `/Debug` и др.).
- **Глобальная настройка «После запуска базы или конфигуратора»** (обе платформы) в **Настройки → Настройки**: сразу после успешного запуска информационной базы или конфигуратора 1С окно приложения можно **свернуть в трей** или **закрыть программу** (полностью завершить работу приложения); значение «Ничего» (по умолчанию) оставляет окно как есть. Работает для запуска из главного окна, по горячим клавишам избранного и из меню трея.

### 🌍 Локализация
- **Многоязычный интерфейс**: **русский** и **английский** из коробки; выбор языка в настройках (применяется сразу), автоопределение по языку ОС при отсутствии явного выбора.
- **Загрузка других языков без пересборки**: достаточно положить `*.json` (формат как у встроенных) в папку `Languages` рядом с приложением или в каталог данных — язык появится в списке выбора.
- При отсутствии перевода строки выполняется откат (английский → русский → ключ), поэтому интерфейс не «ломается» при неполном переводе. Подробнее — [`Localization/README.md`](Configuration Management/Localization/README.md).
- Полностью локализованы диалоговые окна **«Создание информационной базы»** ([`CreateInfobaseWindow.xaml`](Configuration Management/CreateInfobaseWindow.xaml)) и **«Удаление информационной базы»** ([`DeleteInfobaseWindow.xaml`](Configuration Management/DeleteInfobaseWindow.xaml)), компонент **`?`-подсказки** ([`HelpLink.xaml`](Configuration Management/Controls/HelpLink.xaml)), остатки главного окна (подсказки `HelpLink`, строка «Нет выбора») и **текст-заглушка поля поиска** во всех темах ([`DarkTheme.xaml`](Configuration Management/Themes/DarkTheme.xaml), [`LightTheme.xaml`](Configuration Management/Themes/LightTheme.xaml)). Добавлены ключи `HelpLink.*`, `DeleteInfobase.*`, `CreateInfobase.*`, `Main.*` в [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- Дополнительно локализованы оставшиеся строки в Avalonia-контролах и код-бихайндах окон настроек: компонент **`?`-подсказки** (`HelpLink.Avalonia.cs`), поле горячей клавиши **`HotkeyBox`** (WPF и Avalonia), радио сортировки «А → Я» / «Я → А» в окнах **«Выбор группы»** ([`GroupPickerWindow.Avalonia.cs`](Configuration Management/GroupPickerWindow.Avalonia.cs)) и **«Выбор версии платформы»** ([`PlatformVersionPickerWindow.Avalonia.cs`](Configuration Management/PlatformVersionPickerWindow.Avalonia.cs)), «Авто» разрядности сессии и названия начертаний шрифта и встроенных тем в окне настроек. Добавлены ключи `Common.SortAsc/SortDesc`, `Hotkey.Tooltip`, `Theme.Light/Dark`, `Settings.Font.Style*` в [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- Локализованы **диалоговые сервисы** ([`IDialogService.cs`](Configuration Management/Services/IDialogService.cs), [`WpfDialogService.cs`](Configuration Management/Services/WpfDialogService.cs), [`AvaloniaDialogService.cs`](Configuration Management/Services/AvaloniaDialogService.cs)): заголовки сообщений и подтверждений (`Common.Information/Warning/Error/Confirm`), заголовки файловых диалогов (`Dialog.OpenFile/SaveFile/SelectFolder`), фильтр «Все файлы (*.*)» (`Common.AllFiles`) и кнопка «Отмена» (`Common.Cancel`). **Точки входа приложения** ([`App.xaml.cs`](Configuration Management/App.xaml.cs), [`App.axaml.cs`](Configuration Management/App.axaml.cs)): фатальные сообщения через ключи `App.Fatal.*`. Значения по умолчанию параметров интерфейса переведены на пустые строки — локализованный заголовок подставляется в реализациях.
- **Подсказки (ToolTips) кнопок и переключателей главного окна** полностью локализованы в обеих реализациях: WPF — `{loc:Loc Main.*}` (в т.ч. через `MultiBinding` с хоткеем), Avalonia/Linux — `LocalizationManager.T("Main.*Tooltip")` в [`BuildTopBar()`](Configuration Management/MainWindow.Avalonia.cs), [`RefreshColumnHeader()`](Configuration Management/MainWindow.Avalonia.cs) и [`BuildRightPanel()`](Configuration Management/MainWindow.Avalonia.cs); жёстко зашитых русских строк в тултипах нет. Сегментым переключателям «Все / Избранное / Недавние» в Avalonia добавлены подсказки (`Main.AllBasesTooltip`/`Main.FavoritesTooltip`/`Main.RecentTooltip`) для согласования с WPF. Ключи `Main.*Tooltip` согласованы между [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- Локализованы **строка состояния, сообщения диалогов и тултипы главной ViewModel**: Avalonia/Linux ([`MainViewModel.Avalonia.cs`](Configuration Management/ViewModels/MainViewModel.Avalonia.cs)) — строка состояния (`Main.Ready/StatusBase/StatusGroup/LoadedBases`), тултип правой панели (`Main.CollapseRightPanel/ExpandRightPanel`), история запуска (`Main.LaunchAction`), сообщения диалогов (`Main.EditBaseInfo/AddBaseInfo/ConfirmDeleteBase/RefreshConfigInfoMsg/ErrOpenBaseFolder/ShortcutCreated/ErrShortcutCreate/ErrStartStarter/ErrLoadBases`) и синхронизация с ibases.v8i (`Sync.ChooseIbasesFile/IbasesFilter/Completed/ImportedCount/ErrSyncFailed/Failed`); WPF ([`MainViewModel.cs`](Configuration Management/ViewModels/MainViewModel.cs)) — слова строки состояния (`Main.StatusPort/StatusPlatform/StatusUser`) и сообщения синхронизации (`Sync.PrefixExported/PrefixImported/ExportError/ImportError/AddedBases/UpdatedBases/RemovedBases/Skipped/GroupsCreated`). Технические значения сессии и логи не затрагивались.
- Локализованы **отображаемые свойства моделей данных** ([`Infobase.cs`](Configuration Management/Models/Infobase.cs)): разрядность `ArchitectureDisplay` (`Infobase.ArchX64/X86/Priority64/Priority32`), тип подключения и статус базы (`Infobase.Type.*`, `Infobase.Status.*`, `Infobase.Unavailable.*`), режим запуска (`Infobase.LaunchMode.Auto`), «Не запускалась» (`Infobase.LastLaunch.Never`), история запусков (`Infobase.History.Empty/Summary`), единицы размера (переиспользован `CacheClean.SizeUnits`); группа «Без группы»/«Закреплённые» (`Group.NoGroup`, `Main.Pinned`). В редакторе тем ([`ColorScheme.cs`](Configuration Management/Models/ColorScheme.cs)) названия цветов переведены через ключи `Color.*`. Технические хранимые значения не изменены. Ключи `Infobase.*`, `Group.*`, `Color.*` добавлены в [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- **Локализованы специальные узлы дерева групп** — «Закреплённые», «Все базы» и «Без группы» ([`GroupNodeViewModel.cs`](Configuration Management/ViewModels/GroupNodeViewModel.cs), [`GroupNodeViewModel.Avalonia.cs`](Configuration Management/ViewModels/GroupNodeViewModel.Avalonia.cs), [`MainViewModel.cs`](Configuration Management/ViewModels/MainViewModel.cs), [`MainWindow.xaml.cs`](Configuration Management/MainWindow.xaml.cs)). Введён внутренний маркер спец-узла (`Pinned`/`AllBases`/`NoGroup`); отображаемое имя `DisplayName` вычисляется через `LocalizationManager.T(...)` (ключи `Main.Pinned`, `Main.FlatAllBases`, `Group.NoGroup`) и обновляется при смене языка. Логика сравнений (выбор иконки, определение узла «Без группы»/«Закреплённые», DnD) переведена на маркер и не зависит от языка. Добавлено свойство `NodeKey` для языка-независимого сохранения состояния развёрнутости.
- Вынесены в локализацию **оставшиеся сообщения об ошибках создания ИБ `CREATEINFOBASE` в Linux-реализации запуска** ([`OneCLauncher.Linux.cs`](Configuration Management/Services/OneCLauncher.Linux.cs)): ошибка создания каталога (переиспользован `Launcher.CreateDirCreateFailedFormat`) и ошибка запуска `CREATEINFOBASE` (ключ `Launcher.CreateProcessErrorFormat`). Ключи добавлены в [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- Локализовано **сообщение исключения «Резервная копия не найдена.»** ([`IbasesBackupService.cs`](Configuration Management/Services/IbasesBackupService.cs)): текст подставляется в ошибку восстановления резервной копии (`Settings.Ibases.RestoreFailed`) через новый ключ `Settings.Ibases.BackupNotFound`. Ключ добавлен в [`ru.json`](Configuration Management/Localization/Languages/ru.json) и [`en.json`](Configuration Management/Localization/Languages/en.json).
- **Названия колонок главного окна** полностью локализованы в обеих реализациях: WPF — `{loc:Loc Column.*}` в [`MainWindow.xaml`](Configuration Management/MainWindow.xaml), Avalonia/Linux — `LocalizationManager.T("Column.*")` в [`MainWindow.Avalonia.cs`](Configuration Management/MainWindow.Avalonia.cs). Ключи `Column.*` согласованы между `ru.json`/`en.json` (Name, Platform version, Configuration, Launch mode, Server/Base, Last launch, Size); жёстко зашитых названий колонок нет.
- Из форматирования размеров **убраны жёстко прописанные русские fallback-единицы** («Б,КБ,МБ,ГБ,ТБ») в [`CacheCleanWindow.xaml.cs`](Configuration Management/CacheCleanWindow.xaml.cs), [`CacheCleanWindow.Avalonia.cs`](Configuration Management/CacheCleanWindow.Avalonia.cs) и [`Infobase.cs`](Configuration Management/Models/Infobase.cs): единицы теперь берутся исключительно из локализованного ключа `CacheClean.SizeUnits`.
- **Локализованы суффиксы шаблонов конфигураций** «(демо)» / «(пустая)» ([`OneCTemplateService.cs`](Configuration Management/Services/OneCTemplateService.cs), [`CreateInfobaseWindow.xaml.cs`](Configuration Management/CreateInfobaseWindow.xaml.cs)): добавление суффиксов к имени шаблона и их отсечение при подсказке имени базы выполняются через ключи `Template.SuffixDemo` / `Template.SuffixEmpty`. Добавлены в [`ru.json`](Configuration Management/Localization/Languages/ru.json) (`" (демо)"` / `" (пустая)"`) и [`en.json`](Configuration Management/Localization/Languages/en.json) (`" (demo)"` / `" (empty)"`). Детекция «demo/демо», «empty/пуст» по именам каталогов не изменялась.
- **Локализованы имена встроенных тем оформления «Светлая» и «Тёмная»** ([`SettingsWindow.xaml.cs`](Configuration Management/SettingsWindow.xaml.cs)). В выпадающем списке тем окна настроек отображаются переводимые имена через ключи `Theme.Light` / `Theme.Dark`, тогда как канонические идентификаторы (`BuiltInLightName`/`BuiltInDarkName`, значения `ColorScheme.CreateLight()`/`CreateDark()`) остаются неизменными и используются для сохранения/сравнения тем на диске. Кастомные пользовательские темы отображаются без перевода.
- **Локализовано чтение назначенных горячих клавиш** ([`SettingsWindow.xaml.cs`](Configuration Management/SettingsWindow.xaml.cs)). При чтении пустого назначения метод `ReadHotkeyBox(...)` сравнивает значение с локализованным ключом `Common.None` вместо жёстко прописанного русской строки «Нет», поэтому распознавание «нет горячей клавиши» корректно работает при любом языке интерфейса (русский/английский).
- **Тексты кнопок правой панели главного окна** полностью локализованы в обеих реализациях: WPF — `{loc:Loc Main.*}` в [`MainWindow.xaml`](Configuration Management/MainWindow.xaml), Avalonia/Linux — `LocalizationManager.T("Main.*")` в методе [`BuildRightPanel()`](Configuration Management/MainWindow.Avalonia.cs) ([`PrimaryActionButton`](Configuration Management/MainWindow.Avalonia.cs)/[`SecondaryActionButton`](Configuration Management/MainWindow.Avalonia.cs)). Локализованы подписи кнопок, заголовки секций, тексты split-кнопок и пункты их контекстных меню; жёстко зашитых русских строк в текстах кнопок нет. Ключи `Main.*` согласованы между `ru.json`/`en.json`; исправлен перевод `Main.SessionClientOrdinary` («Thin (managed)» → «Ordinary»).
- **Финальный аудит прочих непереведённых строк интерфейса по всему проекту** (окна, контролы, сервисы, ViewModel, обе реализации WPF/Avalonia): проверено regex-поиском по кириллице в строковых литералах C# (`*Window.Avalonia.cs`, `*Window.xaml.cs`, `*ViewModel*.cs`, `Services/*.cs`, `Controls/*.cs`) и по содержимому XAML/AXAML вне `{loc:Loc …}`/`Content=`; комментарии игнорировались. **Жёстко зашитых пользовательских строк не обнаружено** — весь пользовательский текст уже вынесен в локализацию. Оставшиеся русские строки — технические (внутренние ключи режимов запуска/клиента, сопоставляемые с переводимыми подписями через маппинги), строки журналов и диагностики — намеренно не изменены, чтобы не сломать логику хранения/сопоставления.

### 💾 Резервное копирование и восстановление профиля
- **Сохранение всех настроек в произвольный каталог и восстановление после переустановки системы** (обе платформы): вкладка **«Резервное копирование»** в окне настроек позволяет одной кнопкой сохранить весь «профиль» приложения — настройки интерфейса (`settings.json`), список информационных баз вместе с **пользователями и паролями запуска** (`infobases.json`), дерево групп (`groups.json`) и стандартный файл списка баз 1С `ibases.v8i`. После переустановки системы достаточно указать тот же каталог и нажать **«Восстановить профиль»**, либо включить флажок **«Восстанавливать профиль при запуске»** — тогда приложение при каждом запуске само копирует файлы профиля обратно и сразу открывается привычно настроенным, без ручных действий.
- Реализовано единым кросс-платформенным сервисом [`ProfileBackupService.cs`](Configuration Management/Services/ProfileBackupService.cs); источник и цель `ibases.v8i` определяются по настроенному пути или стандартному месту 1С.

### 🎛️ Интерфейс и плотность
- **Компактный режим** (обе платформы): переключатель доступен и **в правом верхнем углу главного окна**, и в **Настройки → Настройки**. Уменьшает размеры иконок и кнопок, сокращает отступы, внутренние поля секций-карточек и расстояния между элементами, а также ширину правой панели — убирая избыточное пустое пространство. Изменение применяется сразу и сохраняется. В компактном режиме дополнительно уменьшаются **шрифт имени базы 1С**, **шрифт имени групп**, **высота оформления групп** и **расстояние между группами**, а **ширина правой панели** максимально сжимается по длине названий команд и выпадающих меню.
- **Настройка шрифта применяется ко всему интерфейсу, включая дерево групп и список баз**: у строк дерева убраны жёсткие размеры шрифта (элементы наследуют применяемый шрифт), а шрифты отдельных областей (кнопки, поля ввода, дерево групп, правая панель, статус-бар) применяются и при запуске приложения.
- **Выбор языка интерфейса** находится во вкладке **«Настройки»** окна настроек.
- **Кнопка «Скопировать техническую информацию»** во вкладке **«О программе»** окна настроек (обе платформы): одним нажатием в буфер обмена копируется **обезличенный** технический отчёт — версия приложения, тип интерфейса (WPF/Avalonia), ОС, архитектура, среда выполнения .NET, число логических процессоров, объём физической памяти и памяти процесса, язык/регион. Личные данные (имя пользователя, имя компьютера, домен, сетевые адреса, пути профилей) намеренно исключены — это удобно передать разработчику для диагностики проблемы. Реализовано единым кросс-платформенным сервисом [`TechnicalInfoService.cs`](Configuration Management/Services/TechnicalInfoService.cs).

### 🗂️ Управление списком баз
- **Вкладки фильтра**: **Все базы**, **Избранное**, **Недавние** (по дате последнего запуска).
- Иерархический список с группами, закреплёнными базами и узлом «Без группы».
- Колонки: наименование, версия платформы, режим запуска, сервер/база, последний запуск, размер; видимость колонок настраивается, **ширину любой колонки можно изменять** перетаскиванием разделителя на её правой границе (данные и заголовок двигаются синхронно). При вложенных группах **колонки данных остаются выровненными с заголовками**; сдвигается только блок ★ / 📌 / название (под иконкой родительской группы).
- **Цветные иконки статуса баз** слева от названия показывают тип подключения и доступность базы: папка **янтарного** цвета — файловая база, глобус **синего** цвета — база на веб-сервере, сеть **фиолетового** цвета — клиент-серверная база, **красный крест** — база недоступна (для файловых — каталог/`1Cv8.1CD` не найден на диске; для клиент-серверных и веб-баз — не заполнены параметры подключения). При наведении на иконку показывается подсказка со статусом. Сетевая доступность удалённо не проверяется автоматически, чтобы не блокировать интерфейс.
- **Кнопка «Доступность» (иконка-сонар) в верхней панели команд** (обе платформы): по нажатию проверяет доступность всех баз по факту и сразу обновляет их иконки в списке — недоступные базы помечаются красным крестиком. Для файловых баз доступность определяется наличием каталога/файла по пути, для клиент-серверных — реальной попыткой подключения через COM-коннектор, для веб-баз — заполненностью адреса. Проверка выполняется в фоне, чтобы не блокировать интерфейс; после завершения в **нижней строке состояния** на 10 секунд показывается сообщение «Проверка доступности: всего {0}, недоступно {1}».
- **Сортировка по клику** на заголовки колонок (название, дата и др.).
- Добавление, редактирование и удаление баз; мастер «Что добавить?» (база или группа).
- **Избранное** (★) и **закрепление** (📌) в одном блоке с названием, без лишних промежутков; закреплённые — в отдельной группе сверху.
- **Поиск** по имени, описанию, группе, версии и тегам (с debounce, без «тормозов» на каждый символ).
- **Теги**: инлайн-добавление в строке базы, компактные чипы, удаление крестиком, **мультифильтр** (несколько тегов сразу) и облако тегов на панели отбора.
- Во вкладках **«Избранное»** и **«Недавние»**, при **отборе по тегу** и при **поиске** группы и закреплённые базы **временно скрываются** — показывается единый плоский список найденных баз, что исключает дубли (закреплённая база раньше попадала и в «Закреплённые», и в свою группу). При возврате к вкладке «Все базы» дерево групп и закрепления восстанавливаются.
- Перетаскивание баз и групп; сворачивание/разворачивание всех групп с верхней панели.
- **Значок тега** в строке заголовка списка (слева от колонки «Название») включает/выключает **только теги в строках списка баз** и не затрагивает панель быстрого отбора тегов. **Верхняя кнопка «Показывать теги»** на верхней панели включает/выключает и **панель тегов сверху** (облако тегов), и **теги в списке баз**; её состояние отражает видимость панели тегов сверху и остаётся включённой, пока панель тегов отображается. Панель также управляется настройкой в окне настроек. При **отключении показа тегов** верхней кнопкой выбранные теги **фильтра списка баз** автоматически **сбрасываются**, чтобы отбор по тегу не продолжал скрывать базы.
- **Навигация стрелками** по списку: `↑`/`↓` — переход по строкам (базам и группам), `←`/`→` — сворачивание/разворачивание выбранной группы. Стрелки всегда управляют выделением в дереве и не перескакивают на кнопки внутри строки.
- **Приложение запоминает последнюю выделенную строку списка** (базу или группу) и восстанавливает её при следующем запуске.
- **Запоминание окна**: приложение запоминает размер, позицию, состояние и **монитор**, на котором было закрыто главное окно, и восстанавливает их при следующем запуске. При нескольких мониторах окно снова открывается на том же экране. Настройка **«Запоминать расположение, размер и монитор окна»** находится в **Настройки → Настройки → Поведение приложения**; при отключении окно при запуске открывается по центру экрана.

### 🏷️ Группы
- Вложенные группы («группа в группе») с **наглядным смещением** кнопки разворота и заголовков групп по глубине вложенности — всегда видно, какая группа находится внутри другой, а также цвет и **иконка** группы. Названия баз с ★/📌 располагаются под иконкой папки родительской группы; колонки данных не смещаются.
- **Сортировка групп по имени** кнопками в заголовке списка (рядом с «Развернуть/Свернуть все группы»): **по возрастанию (А→Я)** и **по убыванию (Я→А)**. Кнопки в стиле **Material Design** сортируют подгруппы рекурсивно, включая вложенные группы; направление запоминается и применяется при перестройке дерева.
- В правой панели при выборе группы отображается **выбранная иконка**.
- Создание, редактирование, удаление; **выбор родительской группы** — в окне, оформленном как выбор версии платформы (зелёная «Выбрать», «Отмена» с красной обводкой, двойной щелчок — подтверждение), где у каждой группы виден её **цвет** и **иконка** из настроек группы (иконка отображается на цвете самой группы — как в списке баз, поэтому видна в обеих темах); сделать группу корневой можно пунктом «Корневая группа».
- Окно группы разделено на **горизонтальные вкладки** «Основные / Цвет / Иконка» по аналогии с окном настроек; кнопки «Сохранить»/«Отмена» всегда закреплены внизу (зелёная «Сохранить», «Отмена» с красной обводкой). Высота окна автоматически подстраивается под содержимое активной вкладки, чтобы **все данные помещались на экран**.
- **Группы без названия** (например, импортированные из `ibases.v8i`) можно создавать и редактировать; в дереве такие группы отображаются как «Без названия».
- **Пустые группы** (без баз) отображаются в дереве в правильной иерархии — внутри своей родительской группы. Переключатель рядом с «показывать группы» на верхней панели и в **Настройки → Отображение → Панели**.
- Оформление узла **«Без группы»** меняется через **«Изменить настройки»** в главном окне, как у обычной группы: открывается то же окно (вкладки **Основные / Цвет / Иконка**), но поля **«Наименование»** и **«Родительская группа»** заблокированы. Изменения сохраняются в настройках приложения.

### 🔄 Импорт, экспорт и синхронизация
- Импорт / экспорт **`ibases.v8i`** (в т.ч. кнопки на главном окне).
- **Синхронизация** с `ibases.v8i`: отключена / только загрузка / только выгрузка / двусторонняя; триггеры — при старте, по интервалу, по расписанию.
- Экспорт / импорт списка в **JSON**; очистка всего списка с подтверждением.
- Автосопоставление **ID (GUID)** базы из `ibases.v8i` для точечной очистки кеша.
- **Резервные копии `ibases.v8i`** перед синхронизацией/экспортом (`ibases.v8i.bak_yyyyMMdd_HHmmss`), хранение N последних копий, кнопка «Восстановить из последней копии» в настройках синхронизации.

### 🧹 Очистка кеша 1С
- Очистка локального кеша платформы для выбранной базы по GUID (программный и/или пользовательский).
- **В окне «Очистка кэша» рядом с типами кеша показывается их текущий размер** — «Программный кэш (1,2 ГБ)», «Пользовательский кэш (340 МБ)». Размер вычисляется асинхронно по содержимому корневых каталогов кеша (на Windows `%LOCALAPPDATA%\1C\1cv8…` и `%APPDATA%\1C\1cv8…`; на Linux `~/.cache/1cv8`, `~/.local/share/1cv8`, `~/.1cv8/1cv8`) и отображается в человекочитаемом виде с единицами, зависящими от языка интерфейса (Б/КБ/МБ/ГБ/ТБ или B/KB/MB/GB/TB).
- **Для каждой базы в списке окна показаны две колонки с размером её кеша — «Программный» и «Пользовательский».** Это позволяет сразу оценить, какая база занимает больше всего места, ещё до очистки.
- **Очистка «остатков» кеша от удалённых баз** — чекбокс **«Остатки от удалённых баз»** с индикатором занимаемого размера в нижней части окна. Он показывает каталоги кеша, не соответствующие ни одной текущей информационной базе (остались после удаления базы из списка или переноса её в другое приложение), и при включении удаляет их вместе с кешем выбранных баз. Очистку остатков можно выполнить и без выбора конкретных баз.
- **Шапка колонок списка закреплена вверху** и остаётся видимой при прокрутке. **Ширину любой колонки можно изменить**, перетаскивая узкую зону захвата на её правой границе (как в главном окне); изменяется только выбранная колонка. Ширины запоминаются при закрытии окна и восстанавливаются при следующем открытии.
- **Split-кнопка «Очистка кеша»** в секции «Обслуживание» правой панели — по аналогии с кнопкой запуска «1С:Предприятие»: прямое нажатие открывает окно очистки кеша, а стрелка «▾» справа открывает выпадающее меню («Открыть окно очистки кеша…», «Программный кеш…», «Пользовательский кеш…», «Программный и пользовательский»). Кнопка доступна даже при выбранной группе: тогда окно очистки открывается без выделенной базы в списке; при выбранной базе она выделяется в окне по умолчанию.

### 🛠️ Создание информационных баз
- Создание ИБ через `CREATEINFOBASE`: пустая база или **из шаблона** (.cf / .dt), файловая и клиент-серверная.
- **Шаблоны конфигураций** — список установленных конфигураций из стандартных каталогов `tmplts` (разбор `1cv8.mft`, как в стартере 1С), группировка в дереве, либо выбор файла вручную.
- **Настройка каталогов шаблонов** в Настройки → Базы (приоритет пути из настроек стартера 1С), кнопка «Из 1С».

### 📦 Обслуживание баз
- **История запусков** (до 30 записей на базу) — пополняется при запуске/выгрузке/тесте, просмотр из контекстного меню.
- **Выгрузка `.dt` / `.cf`** и **тестирование ИБ** (`/IBCheckAndRepair -TestOnly`) через пакетный `DESIGNER`.
- **Анимированный индикатор выгрузки** в верхнем правом блоке кнопок, слева от кнопки синхронизации: иконка-стрелка вверх (выгрузка файла) появляется при запуске выгрузки `.dt` / `.cf`, анимируется (подпрыгивает) до её завершения и автоматически исчезает. При наведении на индикатор показывается **сводка о текущей выгрузке** — операция, имя базы и путь к файлу.
- **Дата-время в имени файла выгрузки** (экспорт списка в **JSON**, выгрузка ИБ в **`.dt`**, выгрузка конфигурации в **`.cf`**): в имя файла автоматически добавляется отметка времени (например `База_20260819_074312.dt`). Включается/выключается флажком **«Добавлять дату-время к имени файла при выгрузке»** в **Настройки → Базы → Список информационных баз** (включён по умолчанию). **Формат отметки настраивается**: рядом с флажком есть выпадающий список **«Шаблон даты и времени»** с предпросмотром примера — можно выбрать готовый шаблон (например `yyyy-MM-dd_HH-mm-ss` → `База_2026-08-19_07-43-12.dt`) или ввести собственный формат `DateTime`.
- **Открыть каталог** файловой базы в проводнике; **ярлык на рабочем столе** (как у стандартного стартера 1С).
- **Удалить отсутствующие файловые базы** (нет каталога / `1Cv8.1CD`) и **завершить процессы 1С** (1cv8, 1cv8c и др.) — Настройки → Базы → Обслуживание.
- Колонка **«Размер»** для файловых ИБ (видимость настраивается в Отображение → Колонки).
- **Удаление ИБ** с опцией **«Физически удалить каталог базы с диска»** (только файловые, с повторным подтверждением).

### 🪟 Системный трей и поведение
- Значок в трее, **закрытие в трей**, восстановление окна двойным кликом.
- Запрет второго экземпляра (опционально) с активацией уже открытого окна.
- Контекстное меню трея: **недавние базы** (до 7) с подменю «1С:Предприятие / Конфигуратор», синхронизация ibases, настройки, выход.
- **Esc → в трей** (настройка «По Esc сворачивать в трей»).
- Кнопка **«Выход»** в правой панели главного окна (в самом низу) **всегда полностью завершает работу приложения**, игнорируя настройку «Закрывать в трей» — в отличие от закрытия окна крестиком, которое при включённой опции сворачивает программу в трей.

### ⚙️ Настройки приложения
- Вкладки: **Платформы**, **Отображение**, **Клавиши**, **Настройки**, **ibases.v8i**, **Базы**, **О программе**.
- Вкладка «Отображение»: подвкладки (Значки и кнопки, Колонки списка, Панели, Нижняя панель) с тумблерами видимости элементов.
- Вкладка «Клавиши»: горячие клавиши запуска и порядок избранного `Alt+1…9`.
- Вкладка «Настройки»: поведение приложения (несколько экземпляров, значок в трее, сворачивание, `Esc` → трей).
- Вкладка «Платформы»: группировка по линии **8.3 / 8.5** и **разрядности** (64/32) с путями установки, **дополнительные пути поиска** платформы.
- Вкладка «Базы»: каталоги шаблонов, экспорт/загрузка списка баз, обслуживание (удаление отсутствующих баз, завершение процессов 1С).
- Вкладка «О программе»: автор, Infostart, GitHub.
- Кнопки **Сохранить** (зелёная) и **Отмена** (красная обводка) — единый стиль диалогов.
- Окно **настроек подключения к базе** оформлено в том же стиле (шапка, те же кнопки) с **вертикальными вкладками слева** — **База / Подключение / Хранилище / Авторизация / Запуск / Разрядность / Платформа / Идентификатор**; каждая вкладка содержит один логический блок без прокрутки. Поле **«Сервер»** на вкладке «Подключение» — выпадающий список с серверами 1С из других клиент-серверных баз списка (можно выбрать или ввести вручную). Поле **«Порт сервера»** — такой же **редактируемый выпадающий список** с портами из существующих клиент-серверных баз (без дублей, по возрастанию): удобно, когда сервер один, а инстансы висят на разных портах.
- **Вкладка «Хранилище»** — хранение адреса сервера, имени, логина и пароля для подключения к хранилищу конфигурации 1С ([`RepositorySettings`](Configuration Management/Models/RepositorySettings.cs)).
- **Вкладка «Авторизация»** содержит две независимые настройки: **«Авторизация в 1С:Предприятие»** (пользователь и пароль для запуска клиента, например тестовый пользователь) и **«Авторизация в Конфигураторе»** (отдельные учётные данные для запуска Конфигуратора, «под собой»). Обе настройки хранятся отдельно и независимо ([`Infobase.EnterpriseAuth`](Configuration Management/Models/Infobase.cs) и [`Infobase.ConfiguratorAuth`](Configuration Management/Models/Infobase.cs)) и **не копируются** друг из друга. При запуске «1С:Предприятие» применяется авторизация Предприятия, при запуске Конфигуратора — авторизация Конфигуратора; для баз, сохранённых до появления этих настроек, используется авторизация базы (обратная совместимость).
- **«Вставить строку подключения»** — кнопка **на вкладке «Подключение»** окна настроек базы (доступна при создании новой базы и при правке свойств, **только на этой вкладке**). По клику открывается **окно ввода строки подключения** с полем для ввода и кнопкой **«Вставить из буфера»**; если в буфере обмена лежит строка, удовлетворяющая критериям ссылки на базу 1С (строка подключения с `Srvr`/`Ref`/`File`/`WS`, ссылка-протокол `e1c://`, веб-адрес `http(s)://`, путь к файловой базе или `server\base`), она **автоматически подставляется** в поле. Кнопка **«Применить»** разбивает строку по полям — тип подключения, сервер/порт, имя базы, путь файла или URL веб-публикации, пользователь и пароль. Если наименование базы не задано, подставляется имя базы (`Ref`) или каталога файловой базы. При открытии свойств существующей базы поле заполняется её текущей строкой подключения.

### 🎨 Интерфейс
- **Гиперссылки-вопросы «?»** — круглый значок «?» рядом с ключевыми и неочевидными элементами форм. По клику открывается всплывающая подсказка, которая объясняет поведение элемента и как с ним взаимодействовать (закрывается повторным кликом или кликом в любом другом месте окна). Такие подсказки добавлены в главное окно, список баз, панель тегов, настройки (клавиши, синхронизация), настройки подключения базы, мастер добавления, окна группы/выбора родителя, создание/удаление ИБ и конфигуратор параметров запуска.
- **Светлая** и **тёмная** темы; переключение с главного окна, а также полноценное **цветовое оформление** в **Настройки → Цветовое оформление**: выбор темы (встроенные «Светлая»/«Тёмная» и собственные), изменение отдельных цветов интерфейса (акцент, боковая панель, фон карточек, текст, границы, строки, полосы прокрутки и др.) с предпросмотром, создание/переименование/удаление своих тем, **выгрузка и загрузка цветовых схем** в JSON-файл. Изменения применяются сразу, активная схема сохраняется в настройках.
- Верхняя панель в один ряд: группировка / развернуть / свернуть · поиск с чипами тегов · вкладки списка · синхронизация ibases · тема · настройки.
- **Правая панель**: карточка базы или группы, действия запуска с отображением хоткеев, изменение, избранное, закрепление, очистка кеша, удаление. Блок **«Текущая сессия»** — режим клиента и разрядность без изменения настроек базы.
- **Компактный режим** правой панели — только кнопки действий (ширина по содержимому).
- **Нижняя панель** (статус): путь/сервер, порт, платформа, разрядность, режим запуска, тип клиента, тип подключения, пользователь, ID базы (настройка видимости).
- Современные стили кнопок, ComboBox, GroupBox; виртуализация списка для больших каталогов баз.
- **Настройка шрифта интерфейса** в **Настройки → Отображение → Шрифт**: выбор семейства шрифта (Segoe UI, Arial, Calibri, Tahoma, Verdana и др.), размера (как в Microsoft Word — от 8 до 72, размер можно также **ввести вручную**) и начертания (Обычный / Полужирный / Курсив / Полужирный курсив) с предпросмотром. Шрифт можно настроить **отдельно для каждой области**: «По умолчанию», список баз, заголовки списка, правая панель, нижняя панель (статус), вкладки, кнопки, поля ввода. Изменения применяются ко всей программе сразу (кнопка «Применить») и сохраняются кнопкой «Сохранить», а также восстанавливаются при следующем запуске приложения.
- Сохранение размеров окна, темы, свёрнутых групп, настроек отображения и шрифта между запусками.

### ⚡ Производительность
- Перестройка дерева только при необходимости; добавление/удаление тега без полной пересборки UI.
- Отложенное сохранение на диск; debounce поиска.
- Фильтрация тегов через `HashSet`; обновление облака тегов только при реальном изменении набора.

---

## 🖥️ Технологии

| Технология | Назначение |
|------------|------------|
| **.NET 10** | Платформа разработки (`net10.0-windows` / `net10.0`) |
| **WPF (Windows Presentation Foundation)** | Графический интерфейс (Windows) |
| **Avalonia 11.3** | Графический интерфейс (Linux): `Avalonia`, `Avalonia.Desktop`, `Avalonia.Themes.Fluent`, `Avalonia.Fonts.Inter` |
| **MVVM** | Архитектурный паттерн |
| **Microsoft.Extensions.DependencyInjection 9.0** | Dependency Injection |
| **MaterialDesignThemes 5.2.1** | Иконки и современный интерфейс (PackIcon, только Windows) |
| **System.Text.Json** | Хранение данных в JSON-файлах |
| **System.Management 8.0** | WMI `Win32_Process` (проверка блокировки конфигуратора, только Windows) |
| **Linux CLI (`1cv8`, `readelf`, `xdg-open`, `/proc`)** | Сервисы платформы 1С и файловый менеджер (только Linux) |

---

## 📦 Требования

### Windows
- **Windows 10 / 11**
- **.NET 10 SDK** (для сборки из исходного кода)
- Установленная платформа **1С:Предприятие 8.3** (для запуска баз)

### Linux
- **.NET 10 SDK** (для сборки из исходного кода; self-contained публикация не требует установки runtime)
- Установленная платформа **1С:Предприятие 8.3 для Linux** (клиент `1cv8`, например `/opt/1cv8/<версия>/bin/1cv8`) — для запуска баз
- Зависимости пакета: GTK3, glib2, `xdg-utils` (для открытия каталогов/ссылок). Для трея на GNOME Shell может потребоваться расширение **AppIndicator**

---

## 🐧 Поддержка Linux (портирование)

> **Статус: Linux-порт выполнен (Этапы 0–8).** Реализованы инфраструктура csproj, Avalonia UI (окна `*.Avalonia.cs`, конвертеры `Converters/Avalonia/`, темы `*.axaml`), пути/хранилище (`PlatformPaths.cs`), сервисы платформы 1С (`*.Linux.cs`), ярлыки/файловый менеджер/трей, сборка и упаковка (AppImage, `.deb`). Windows-сборка не сломана (0 ошибок). Остаётся **прогон тестов на Linux-хосте** по чек-листу [`Configuration Management/LINUX_TESTING.md`](Configuration Management/LINUX_TESTING.md). Подробности — в [`Configuration Management/LINUX_PORT.md`](Configuration Management/LINUX_PORT.md) и [`PLAN_LINUX.md`](PLAN_LINUX.md).

### Целевые дистрибутивы и разрядность

| Дистрибутив | Семейство | Архитектура |
|-------------|-----------|-------------|
| **Ubuntu LTS** (22.04+, 24.04+) | Debian (`.deb`) | `x64` (amd64) |
| **Debian** (12+) | Debian (`.deb`) | `x64` (amd64) |
| **ALT Linux** (10, 9) | RPM/ALT (`.rpm`) | `x64` |
| **Astra Linux** (SE 1.7+, Смоленск 1.7+) | Debian (`.deb`) | `x64` |

- Сборка — **64-разрядная** (`linux-x64` / `amd64`); 32-разрядная не поддерживается.
- Минимальная версия: **.NET 10 SDK**; self-contained публикация не требует установки runtime.

### Сборка на Linux

```bash
# Только сборка (Avalonia, net10.0)
./build.sh

# Сборка + публикация self-contained single-file linux-x64
./build.sh Release publish
```

Результат публикации: `publish/linux-x64/` — один исполняемый файл `ConfigurationManagement` (self-contained, не требует установки .NET Runtime).

> Целевой TFM и RID выбираются автоматически по ОС, на которой запускается сборка: на **Windows** — WPF (`net10.0-windows`, RID `win-x64`), на **Linux** — Avalonia (`net10.0`, RID `linux-x64`). Скрипт [`build.sh`](Configuration Management/build.sh) собирает/публикует для текущей ОС (Linux — `linux-x64`, Windows — `win-x64`); на Windows то же выполняет [`build.ps1`](Configuration Management/build.ps1).

### 📦 Установка на Linux

Готовые пакеты собираются из single-file публикации и включают `.desktop`-файл и иконку приложения.

#### AppImage (переносимый)

```bash
# 1) Сборка single-file
./build.sh Release publish

# 2) Упаковка в AppImage (нужен appimagetool в PATH)
./package/linux/appimage.sh
```

Результат: `package/linux/out/ConfigurationManagement-x86_64.AppImage`. Запуск:
```bash
chmod +x package/linux/out/ConfigurationManagement-x86_64.AppImage
./package/linux/out/ConfigurationManagement-x86_64.AppImage
```

#### .deb (Debian/Ubuntu/Astra)

```bash
# 1) Сборка single-file
./build.sh Release publish

# 2) Упаковка в .deb (нужен dpkg-deb)
./package/linux/deb/build-deb.sh
```

Результат: `package/linux/deb/out/configuration-management_<версия>_amd64.deb`. Установка/удаление:
```bash
sudo dpkg -i package/linux/deb/out/configuration-management_*_amd64.deb
sudo apt remove configuration-management   # удаление
```

#### .rpm / snap (необязательно)

Формат `.rpm` (для ALT Linux, Fedora) и `snap` поддерживаются на уровне структуры пакета, но скрипты сборки для них пока не реализованы. `.deb`-пакет может быть пересобран для `.rpm` через `alien` при необходимости.

---

## 🔧 Сборка и запуск

### Из исходного кода

```bash
# Клонирование репозитория
git clone https://github.com/USERNAME/Configuration-Management.git
cd Configuration-Management

# Сборка проекта
dotnet build "Configuration Management/Configuration Management.csproj"

# Запуск
dotnet run --project "Configuration Management/Configuration Management.csproj"
```

Либо откройте файл решения `Configuration Management.slnx` в **Visual Studio 2022+** и запустите проект.

### 📦 Публикация автономного приложения

Проект настроен на публикацию в виде **автономного (self-contained) single-file** исполняемого файла. Для Windows x64 выполните ([`build.ps1`](Configuration Management/build.ps1)):

```powershell
.\build.ps1 -Publish
```

либо вручную:

```powershell
dotnet publish "Configuration Management.csproj" -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true
```

Результат — в каталоге `publish\win-x64\`: один исполняемый файл **`ConfigurationManagement.exe`** (~75 МБ, self-contained single-file со встроенными нативными библиотеками), который не требует установки .NET Runtime на целевой машине.

---

## 📁 Структура проекта

> Проект — **одна кодовая база** с разделением по ОС. WPF-файлы (`.xaml`, `.xaml.cs`) используются на Windows; на Linux вместо них компилируются Avalonia-версии (`*.Avalonia.cs`, `.axaml`, `Converters/Avalonia/`, `Services/*.Linux.cs`). Выбор целевого TFM и файлов выполняется в [`Configuration Management.csproj`](Configuration%20Management/Configuration%20Management.csproj) по ОС.

```
Configuration Management/
├── App.axaml / App.axaml.cs        # Точка входа Avalonia (Linux)
├── App.xaml / App.xaml.cs          # Точка входа WPF (Windows): ошибки, один экземпляр, тема
├── Program.cs                      # Точка входа .NET (Avalonia, Linux)
├── AppServices.cs                  # DI-контейнер (Microsoft.Extensions.DependencyInjection)
├── app.ico / app_icon_preview.png  # Иконки приложения (ICO для Windows, PNG для Linux/трея)
├── tray.ico / tray_icon_preview.png# Иконки системного трея
├── MainWindow.xaml(.cs)            # Главное окно (Windows)
├── MainWindow.Avalonia.cs          # Главное окно (Linux)
├── SettingsWindow.xaml             # Окно настроек (Windows)
├── SettingsWindow.Avalonia.cs      # Окно настроек (Linux)
├── AddEditWindow.xaml              # Мастер добавления (база или группа)
├── ConnectionSettingsWindow.xaml   # Диалог настройки подключения к базе
├── ConnectionStringInputWindow.xaml# Окно ввода строки подключения
├── CacheCleanWindow.xaml           # Очистка кэша 1С (тип кэша + набор баз)
├── CreateInfobaseWindow.xaml       # Создание ИБ (пустая или из шаблона .cf/.dt)
├── DeleteInfobaseWindow.xaml       # Удаление ИБ (в т.ч. физическое удаление каталога)
├── GroupEditWindow.xaml            # Создание/редактирование группы (Основные/Цвет/Иконка)
├── GroupPickerWindow.xaml          # Выбор родительской группы в виде дерева
├── GroupSettingsWindow.xaml        # Настройки группы (цвет/иконка)
├── ColorPickerWindow.xaml          # Диалог выбора цвета (RGB/HEX)
├── TagInputWindow.xaml             # Диалог ввода тега
├── NameInputWindow.xaml            # Ввод названия темы/группы
├── LinkInputWindow.xaml            # Ввод ссылки на базу («Перейти по ссылке»)
├── LaunchParametersWindow.xaml     # Конфигуратор параметров запуска 1С
├── PlatformVersionPickerWindow.xaml# Выбор установленной версии платформы
├── ModalWindowBase.cs              # База модальных окон (обе платформы)
├── Models/                         # Модели данных (чистый .NET, обе платформы)
│   ├── Infobase.cs                 # Информационная база (+ EnterpriseAuth/ConfiguratorAuth/Repository)
│   ├── ConnectionSettings.cs       # Настройки подключения
│   ├── InfobaseAuthSettings.cs     # Отдельная авторизация Предприятия/Конфигуратора
│   ├── RepositorySettings.cs       # Настройки хранилища конфигурации
│   ├── Group.cs                    # Группа баз (иерархия через ParentId)
│   ├── GroupHierarchyHelper.cs     # Вспомогательные методы для иерархии групп
│   ├── MetadataNode.cs             # Дерево метаданных конфигурации
│   ├── OneCConfigInfo.cs           # Информация о конфигурации (наименование, версия)
│   ├── OneCComConnection.cs        # Параметры COM-подключения
│   ├── PlatformVersionGroup.cs     # Группа установленных версий платформы
│   ├── PlatformVersionInfo.cs      # Установленная версия платформы (путь, разрядность)
│   ├── IbasesSyncMode.cs           # Режим синхронизации с ibases.v8i
│   ├── IbasesSyncTrigger.cs        # Триггер синхронизации (старт/интервал/расписание)
│   ├── LaunchKind.cs               # Вид запуска (Предприятие / Конфигуратор)
│   ├── SessionLaunchModes.cs       # Режимы клиента и разрядности текущей сессии
│   ├── ListViewMode.cs             # Вкладки списка (Все / Избранное / Недавние)
│   ├── LaunchHistoryEntry.cs       # Запись истории запусков
│   ├── ElementFontSettings.cs      # Настройки шрифта по областям интерфейса
│   ├── AppSettings.cs              # Настройки интерфейса (колонки, окно, цветовая схема, шрифты, синхронизация)
│   ├── ColorScheme.cs              # Цветовая схема (тема): цвета, встроенные схемы, экспорт/импорт
│   └── InfobaseExportData.cs       # Контейнер экспорта/импорта
├── Themes/                         # Темы оформления
│   ├── ThemeManager.cs             # Применение тем/цветовых схем и шрифтов (Windows)
│   ├── ThemeManager.Avalonia.cs    # То же для Avalonia (Linux)
│   ├── LightTheme.xaml / LightTheme.axaml
│   ├── DarkTheme.xaml / DarkTheme.axaml
│   └── Icons.xaml / Icons.axaml    # Иконки (Geometry)
├── Services/                       # Сервисы
│   ├── OneCLauncher.cs / OneCLauncher.Linux.cs  # Запуск платформы 1С (WPF / CLI `1cv8`)
│   ├── OneCCacheCleaner.cs         # Очистка кэша 1С (программный/пользовательский, по GUID)
│   ├── OneCTemplateService.cs      # Шаблоны конфигураций (разбор 1cv8.mft)
│   ├── IbasesV8iImporter.cs / IbasesV8iExporter.cs # Импорт/экспорт ibases.v8i
│   ├── IbasesBackupService.cs      # Резервные копии ibases.v8i
│   ├── IbasesV8iExporter.cs
│   ├── IIbasesSyncService.cs       # Интерфейс синхронизации с ibases.v8i
│   ├── InfobaseRepository.cs / IInfobaseRepository.cs # Хранение в JSON (атомарная запись)
│   ├── InfobaseMaintenanceService.cs / .Linux.cs # Выгрузка .dt/.cf, тест, ярлык (.lnk / .desktop)
│   ├── PlatformVersionService.cs / .Linux.cs # Поиск версий (Program Files / /opt/1cv8, readelf)
│   ├── PlatformPaths.cs            # Пути данных/кэша/шаблонов (Windows и Linux/XDG)
│   ├── OneCComConnector.cs / .Linux.cs  # COM-коннектор 1С (Windows) / эвристика+DESIGNER (Linux)
│   ├── OneCComConnectorRegistrar.cs # Регистрация COM-компонента (только Windows)
│   ├── ConfigurationInfoService.cs # Запрос информации о конфигурации
│   ├── LinuxProcess.cs             # Чтение командной строки из /proc/<pid>/cmdline (Linux)
│   ├── IOneCLauncher.cs / IPlatformVersionService.cs / IAppLogger.cs / IDialogService.cs
│   ├── FileAppLogger.cs            # Файловое логирование с ротацией
│   ├── WpfDialogService.cs         # Диалоги через MVVM (Windows, без MessageBox в ViewModel)
│   └── AvaloniaDialogService.cs    # Диалоги + StorageProvider (Linux)
├── ViewModels/                     # ViewModel (MVVM)
│   ├── MainViewModel.cs / MainViewModel.Avalonia.cs # Главная ViewModel (фасад)
│   ├── LaunchViewModel.cs / LaunchViewModel.Avalonia.cs # Логика запуска (LaunchCommand)
│   ├── ConnectionSettingsViewModel.cs
│   ├── GroupNodeViewModel.cs / GroupNodeViewModel.Avalonia.cs # Узел дерева групп
│   ├── ViewModelBase.cs / ViewModelBase.Avalonia.cs
│   └── RelayCommand.cs / RelayCommand.Avalonia.cs
├── Controls/                       # Переиспользуемые элементы (WPF + *.Avalonia.cs)
│   ├── GroupTreeView.xaml(.cs)     # Дерево групп
│   ├── InfobaseListView.xaml(.cs)  # Список информационных баз
│   └── HelpLink.xaml(.cs)          # Гиперссылка-подсказка «?»
├── Converters/                     # Конвертеры WPF (Windows)
├── Converters/Avalonia/            # Конвертеры Avalonia (Linux) — 10 шт.
├── build.sh / build-linux-single-file.sh / build.ps1  # Скрипты сборки
├── Configuration Management.csproj # Единый csproj с условной компиляцией по ОС

package/
└── linux/
    ├── appimage.sh                 # Упаковка в AppImage
    ├── configuration-management.desktop
    ├── app.png                     # Иконка приложения для пакетов
    └── deb/                        # Сборка .deb пакета (DEBIAN/control)

tools/
└── GenerateIcon/                   # Утилита генерации иконки приложения
    ├── GenerateIcon.csproj
    └── Program.cs
```

---

## 💾 Хранение данных

Данные приложения сохраняются в каталоге `%APPDATA%\ConfigurationManagement` (на Linux — `~/.config/ConfigurationManagement`, путь определяется в [`PlatformPaths.cs`](Configuration%20Management/Services/PlatformPaths.cs)):

| Файл | Содержимое |
|------|------------|
| `infobases.json` | Список информационных баз |
| `groups.json` | Список групп с цветами |
| `settings.json` | Настройки интерфейса (тема, избранное, группировка, ширина колонок, размер и позиция окна, активная цветовая схема, шрифты, режим синхронизации с `ibases.v8i`) |
| `ColorSchemes/` | Пользовательские цветовые схемы (JSON) |
| `logs/` | Журнал приложения (`app-YYYYMMDD.log`, ротация 14 дней / 5 МБ) |

## 🔗 Работа с файлом `ibases.v8i`

Приложение взаимодействует со стандартным файлом списка баз 1С `ibases.v8i` (расположен в `%APPDATA%\1C\1CEStart\`):

- **Импорт** — считывает базы, группы, ID (GUID), версию платформы (`Version`), режим запуска (`App`), локаль (`Locale`), признак внешней базы (`External`), скорость соединения (`ClientConnectionSpeed`), признак веб-клиента (`WA`) и дополнительные параметры подключения (`AdditionalParameters`, сохраняются в параметры запуска базы). Поддерживает **иерархию групп**: пути вида «Родитель\Дочерняя» разбираются на сегменты, недостающие родительские группы создаются автоматически. Добавляет новые базы и обновляет существующие.
- **Экспорт** — выгружает базы и группы приложения в файл `ibases.v8i`, добавляя новые записи и обновляя существующие (по совпадению имени базы). Группы записываются секциями без строки подключения, для баз переносятся версия платформы, режим запуска (`App`/`DefaultApp`) и дополнительные параметры запуска. Существующие секции-группы только обновляются (новые при выгрузке не создаются), а дубликаты секций с одинаковым именем устраняются (приоритет — за базой). Строка подключения (`Connect`) всегда завершается знаком «;» (например, `Connect=File="D:\база";`) — это необходимо для корректной работы **EDT** и других средств 1С с базой.
- **Синхронизация** — в окне настроек можно задать режим автоматической синхронизации с файлом `ibases.v8i` (отключена / только загрузка / только выгрузка / двусторонняя) и путь к файлу. Синхронизация выполняется автоматически при изменении списка баз, при запуске приложения, через заданный интервал или по расписанию, а также вручную по кнопкам «Загрузить» / «Выгрузить».
- **Сопоставление ID** — при ручном создании базы приложение автоматически находит её ID в `ibases.v8i` по имени или строке подключения и подставляет его. Это обеспечивает корректную точечную очистку кеша 1С по GUID.
- **Очистка кеша** — использует ID базы (GUID) для удаления только соответствующего подкаталога кеша, а не всего каталога базы.

---

## 🧩 Архитектура

Приложение построено по паттерну **MVVM**:

- **Models** — описывают доменные сущности (информационные базы, группы, настройки подключения).
- **ViewModels** — содержат логику представления и команды, реализуют `INotifyPropertyChanged`.
- **Views (XAML)** — отвечают за отображение и привязку данных.
- **Services** — инкапсулируют работу с платформой 1С, файловой системой и хранилищем данных.

---

## 🙏 Благодарности

Существенную часть работ по **Linux/Avalonia-версии** — доведение приложения до сборки, запуска и работы с платформой 1С:Предприятие — выполнил **ksv47**. Все правки, вошедшие в слияние **PR #54** (`ksv47/linux-fixes`), принадлежат авторству **ksv47** (см. записи 0.3.3.12–0.3.3.24 и 0.3.3.27 в [`CHANGELOG.md`](CHANGELOG.md)).

Продолжением стала **перетаскивание баз и групп мышью** в Linux/Avalonia-версии вместе с действием после запуска базы или конфигуратора и сохранением выбора при пересборке дерева — все правки слияния **PR #55** (`ksv47/linux-fixes`) также выполнены автором **ksv47** (см. запись 0.3.3.31 в [`CHANGELOG.md`](CHANGELOG.md)).

Далее были влиты ещё несколько слияний из ветки `ksv47/linux-fixes` (а также `ksv47/linux-fixes-2` для PR #61):

- **PR #56** — освобождение подписок диалогов на ресурсы темы и локализацию, оформление окон сообщений темой, кисти темы через динамический ресурс вместо ручных подписок, прокрутка переживает пересборку дерева (см. запись 0.3.3.45 в [`CHANGELOG.md`](CHANGELOG.md)).
- **PR #58** — выпадающие меню кнопок запуска, вертикальная полоса прокрутки списка, клавиши `Home`/`End`/`PageUp`/`PageDown`, разовый запуск с параметрами и с запросом имени/пароля, чистка задвоенных ключей локализации (см. запись 0.3.3.46 в [`CHANGELOG.md`](CHANGELOG.md)).
- **PR #59** — каталоги шаблонов и операции со списком баз на вкладке «Базы» (выгрузка/загрузка JSON, безопасный импорт из `ibases.v8i`) (см. запись 0.3.3.47 в [`CHANGELOG.md`](CHANGELOG.md)).
- **PR #60** — обслуживание и опасные операции на вкладке «Базы», работоспособные настройки трея, упаковка в deb и AppImage (см. запись 0.3.3.48 в [`CHANGELOG.md`](CHANGELOG.md)).
- **PR #61** — читаемый справочник ключей в параметрах запуска, кнопка тегов, меню трея, показывающее текущее состояние списка, исправление перегрузки `SetProperty` (см. запись 0.3.3.49 в [`CHANGELOG.md`](CHANGELOG.md)).

Все правки этих слияний **#56**, **#58**, **#59**, **#60** и **#61** также выполнены автором **ksv47**. Большое спасибо за вклад!

---

## 🤝 Вклад в проект

Буду рад любым улучшениям! Для внесения вклада:

1. Сделайте форк репозитория.
2. Создайте ветку для вашей фичи (`git checkout -b feature/amazing-feature`).
3. Внесите изменения и закоммитьте их.
4. Отправьте pull request.

---

## 📬 Контакты

Если у вас возникли вопросы или предложения — создайте [issue](https://github.com/USERNAME/Configuration-Management/issues) в репозитории.

## Сборка

### Локально (Windows)

```powershell
# Только сборка
.\build.ps1

# Сборка + публикация single-file win-x64
.\build.ps1 -Publish
```

Результат публикации: `publish\win-x64\`.

Или вручную:

```powershell
dotnet build "Configuration Management.csproj" -c Release
dotnet publish "Configuration Management.csproj" -c Release -r win-x64 --self-contained true -p:PublishSingleFile=true -o .\publish\win-x64
```

> На Windows проект собирается как WPF (`net10.0-windows`); на Linux — как Avalonia (`net10.0`). Целевой TFM и набор компилируемых файлов выбираются автоматически по ОС в [`Configuration Management.csproj`](Configuration%20Management/Configuration%20Management.csproj). Для Linux доступны скрипты `./build.sh`, `./build-linux-single-file.sh` и упаковка в AppImage / `.deb` (см. раздел «🐧 Поддержка Linux»).

### Автоматическая сборка (GitHub Actions)

Workflow'ы CI (`.github/workflows/build.yml`, `.github/workflows/release.yml`) в репозитории пока **не добавлены**. Планируются:

| Workflow | Когда запускается | Что делает |
|----------|-------------------|------------|
| **Build** | push / PR в `main`, `master`, `develop` | Restore → Build Debug/Release → Publish win-x64 → artifact |
| **Release** | тег `v*.*.*` или ручной запуск | Publish + ZIP + GitHub Release |

Создание релиза (после добавления workflow):

```bash
git tag v0.3.3.33
git push origin v0.3.3.33
```

Артефакт `ConfigurationManagement-v0.3.3.33-win-x64.zip` (и `-linux-x64`, при включённом Linux-джобе) появится в Releases.

> Для WPF-сборки требуется Windows-раннер (`windows-latest`); для Linux-сборки Avalonia — `ubuntu-latest`.


